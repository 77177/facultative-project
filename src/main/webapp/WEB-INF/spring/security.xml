<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd">
    
    <http>
        <intercept-url pattern="/authenticator/**" access="isAnonymous()"/>
        <intercept-url pattern="/course**" access="isAnonymous()"/>
        <intercept-url pattern="/**" access="hasAnyRole('admin', 'student', 'teacher')"/>
        <intercept-url pattern="/student**" access="hasAnyRole('admin', 'student')"/>
        <intercept-url pattern="/teacher**" access="hasAnyRole('admin', 'teacher')"/>
        <form-login login-page="/authenticator/login"
                    authentication-failure-url="/"
                    default-target-url="/course"/>
        <logout delete-cookies="JSESSIONID"
                logout-url="/"/>
    </http>

    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="me" authorities="admin" password="{noop}12345"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>